Bài làm thêm về HTTP:
--------------
 HTTP Client
Gọi API https://dummy.restapiexample.com/api/v1/employees và trả về 1 slice các struct có thông tin trong đấy
Viết 1 worker pool dựa trên ví dụ ở https://gobyexample.com/worker-pools, nhưng job là struct về người gồm các thông tin ở câu trên, job trả về lương chia cho tuổi của mỗi người.

---------------

HTTP Server
đọc thêm ở đây: https://go.dev/doc/articles/wiki/
Thiết kế 1 webserver để Get/Set dữ liệu cho cái struct ở phần dữ liệu employee ở trên, yêu cầu là data phải được lưu lại ở file a.txt (không dùng database, mà hãy lưu data trên file). 
Extra if have time (good to have): có thể search được dữ liệu
Có front-end thì càng tốt =)))!

Yêu cầu: support được concurrent & thead safe access